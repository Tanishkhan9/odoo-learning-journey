<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- ==================== Record Rules ==================== -->
        
        <!-- Rule 1: Managers can access all student records -->
        <record id="rule_student_manager_all" model="ir.rule">
            <field name="name">Student Manager - All Records</field>
            <field name="model_id" ref="model_student_student"/>
            <field name="groups" eval="[(4, ref('group_student_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
            <field name="domain_force">[(1, '=', 1)]</field> <!-- All records -->
        </record>
        
        <!-- Rule 2: Users can access only active student records -->
        <record id="rule_student_user_active_only" model="ir.rule">
            <field name="name">Student User - Active Records Only</field>
            <field name="model_id" ref="model_student_student"/>
            <field name="groups" eval="[(4, ref('group_student_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
            <field name="domain_force">[('status', '=', 'active')]</field>
        </record>
        
    </data>
</odoo>
